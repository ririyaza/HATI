Scenario: The Professor's Signature (Fear of Authority)

Drop files here with these exact names:
  background.jpg  - the scene's background image
  npc.png         - the professor sprite (transparent background recommended)

Keep the same filenames and extensions when replacing so no code changes
are needed. After replacing a file, hot-restart (not hot-reload) the app,
or rebuild, to see the change - Flutter caches bundled images.
